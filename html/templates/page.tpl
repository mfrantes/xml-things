<!DOCTYPE html>
<html lang="en-US">
<head>{{ head }}</head>
<body onload="displayResult('{{ xsl }}', '{{ xml }}', '{{ id }}', '{{ mathjax }}')">
	<div id="{{ id }}"></div>
	<div class="container"></div>
	<script src="{{ js }}"></script>
</body>
</html>
